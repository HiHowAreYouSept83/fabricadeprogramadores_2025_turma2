"""numeros=[ 1, 4, 7, 21, 98, 156]
coordenadas=[ 1.5694, 9.3991]
ciaturas_mitologicas=["unicornio", "vampiro", "minotauro", "dragao", "chupacabra"]
print(numeros[3])

print(numeros + coordenadas)"""

"""import math
def equação ():
    a=float(input("insira o valor de A: "))
    b=float(input("insira o valor de B: "))
    c=float(input("insira o valor de C: "))
    delta = b**2 - 4 * a * c 
    if delta >=0: 
        x=( -b + math.sqrt(delta)) /2 * a
        y=( -b - math.sqrt(delta)) /2 * a
        raiz_quadrada = math.sqrt(delta)
        print("a raiz quadrada de", "é", raiz_quadrada)
        print("o resultado é ", x, y)
        #calculo da raiz real
    else:
        print("essa equação não possui raizes reais")
equação()  """

"""import random 
numero_aleatorio = random.randint(0,10)
print(numero_aleatorio)"""

"""import random 
cesta =['mustang', 'jaguar', 'stratocaster', 'telecaster', 'jazzmaster', 'les-paul', 'semi-acustica', 'jagstang', 'flying-v', 'explorer']
print(random.choice(cesta))

import time
print(time.asctime())

import turtle
turtle.circle(50)
turtle.getscreen()._root.mainloop()"""

import this